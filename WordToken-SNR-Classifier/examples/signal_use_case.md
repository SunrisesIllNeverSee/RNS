# Example: Signal Use

Sentence: "The MOS²ES system encodes lineage into every keystroke."

In this case:
- 'MOS²ES', 'system', 'encodes', 'lineage', 'keystroke' → signal (high semantic density)
- 'the', 'into', 'every' → support (not noise, but neutral scaffolding)
